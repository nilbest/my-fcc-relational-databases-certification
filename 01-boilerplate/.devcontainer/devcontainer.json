{
  "name": "RDB Alpha",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
//    "args": {
//      "CODEROAD_WEBHOOK_TOKEN": "${localEnv:CODEROAD_WEBHOOK_TOKEN}"
//    }
//  },
//  "containerEnv": {
//    "CODEROAD_WEBHOOK_TOKEN": "${localEnv:CODEROAD_WEBHOOK_TOKEN}"
  },
  //"dockerFile": "../Dockerfile",
  "workspaceFolder": "/workspace/project",
  "envFile": "../.env",
  "remoteEnv": {
    "CODEROAD_WEBHOOK_TOKEN": "${localEnv:CODEROAD_WEBHOOK_TOKEN}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "coderoad.coderoad"
      ]
    }
  }
}